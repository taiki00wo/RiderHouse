<div class='riderhouse-bg'>
  <div class='bg-mask'>
    <div class="container">
      <div class="row">
        <div class="col-lg-6">
    
          <% if @riderhouse.image? %>
            <%= image_tag @riderhouse.image.url, :size =>'500x375' %>
          <% else %>
            <div class="image-box"%></div>
          <% end %> 

          <table class="table">
            <tbody>

              <%= form_with model: @riderhouse, local: true do |f| %>
              <%= render 'layouts/error_messages', model: f.object %>
                <tr>
                  <td><%= f.label "画像をアップロード" %></td>
                  <td><%= f.file_field :image %></td>
                </tr>

                <tr>
                  <td><%= f.label :name, "名称" %></td>
                  <td><%= f.text_field :name, class: 'form-control' %></td>
                </tr>

                <tr>
                  <td><%= f.label :address, "住所" %></td>
                  <td><%= f.text_field :address, class: 'form-control' %></td>
                </tr>

                <tr>
                  <td><%= f.label :tel, "電話番号" %></td>
                  <td><%= f.text_field :tel, class: 'form-control' %></td>
                </tr>

                <tr>
                  <td><%= f.label :price, "価格" %></td>
                  <td><%= f.text_field :price, class: 'form-control' %></td>
                </tr>

                <tr>
                  <td><%= f.label :opening_hours, "営業時間" %></td>
                  <td><%= f.text_field :opening_hours, class: 'form-control' %></td>
                </tr>

                <tr class="no-underline">
                  <td>
                    <%= link_to riderhouse_path(@riderhouse) do %>
                      <i class="fas fa-backspace fa-5x"></i>
                    <% end %>
                  </td>
                  <td>
                    <%= button_tag type: 'submit' do %>
                      <i class="fas fa-save fa-5x"></i>
                    <% end %>
                  </td>
                </tr>
              <% end %> 
            </tbody>
          </table> 
        </div>
      </div>
    </div>
  </div>
</div>